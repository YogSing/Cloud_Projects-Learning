sha1sum /dev/zero &
commands for every core in your system.

To stop killall sha1sum
or
Copy kill sha1sum
# This script will run sha1sum on /dev/zero in the background
# for every core in your system.
# It will run until you kill it.
# It will use all the CPU cores available.
